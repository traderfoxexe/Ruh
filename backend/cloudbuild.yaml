# Cloud Build configuration for automatic deployment to Cloud Run
# This file is triggered when code is pushed to the connected repository

steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/ruh-api:$COMMIT_SHA', '-t', 'gcr.io/$PROJECT_ID/ruh-api:latest', '.']
    dir: 'backend'

  # Push the Docker image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ruh-api:$COMMIT_SHA']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ruh-api:latest']

  # Deploy to Cloud Run using service.yaml
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'services'
      - 'replace'
      - 'service.yaml'
      - '--region'
      - 'us-central1'
    dir: 'backend'

images:
  - 'gcr.io/$PROJECT_ID/ruh-api:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/ruh-api:latest'

options:
  logging: CLOUD_LOGGING_ONLY
